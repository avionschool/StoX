<% if current_user.role.name == 'Broker'  %>
    <h1>Broker's Portal</h1>
<% elsif current_user.role.name == 'Buyer' %>
    <h1>Buyer's Portal</h1>
<% elsif current_user.role.name == 'Admin' %>
    <h1>Admin's Portal</h1>
<% end %>
<hr>
<% if current_user.role.name == 'Broker' || current_user.role.name == 'Buyer' %>
    <%= link_to 'My Portfolio', my_portfolio_path %>
<% end %>
<br>
<%= link_to('Logout', destroy_user_session_path, method: :delete) %>  

